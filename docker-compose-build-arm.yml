version: "3.3"

services:
  shmwrapper:
    image: comworkio/shmwrapper
    container_name: shmwrapper
    build:
      context: .
      dockerfile: docker/build/Dockerfile.arm
      target: shmwrapper
